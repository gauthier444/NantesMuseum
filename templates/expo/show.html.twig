{% extends 'base.html.twig' %}

{% block title %}Expositions{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/expo_index.css') }}">
    <link href="https://fonts.googleapis.com/css?family=Playfair+Display+SC" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Chakra+Petch" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.0/css/bootstrap.min.css" integrity="sha384-PDle/QlgIONtM1aqA2Qemk5gPOE7wFq8+Em+G/hmo5Iq0CCmYZLv3fVRDJ4MMwEA" crossorigin="anonymous">
{% endblock %}

{% block body %}
    <h1>Exposition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ expo.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ expo.Description }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ expo.Location }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ expo.Date ? expo.Date|date('d/m/Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="link">
        <a href="{{ path('expo_index') }}">Liste des expositions</a>
    </div>

    {{ include('expo/_delete_form.html.twig') }}
{% endblock %}
