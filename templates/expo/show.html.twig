{% extends 'base.html.twig' %}

{% block title %}Expo{% endblock %}

{% block body %}
    <h1>Expo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expo.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ expo.Description }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ expo.Location }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ expo.Date ? expo.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expo_index') }}">back to list</a>

    <a href="{{ path('expo_edit', {'id': expo.id}) }}">edit</a>

    {{ include('expo/_delete_form.html.twig') }}
{% endblock %}
