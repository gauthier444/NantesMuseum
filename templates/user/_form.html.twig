
{% block stylesheets %}
    <link href="https://fonts.googleapis.com/css?family=Playfair+Display+SC" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/user.css') }}">
{% endblock %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Validez') }}</button>
{{ form_end(form) }}

